<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.steeringhub.spec.mapper.SpecMapper">

    <resultMap id="specResultMap" type="com.steeringhub.spec.entity.Spec">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="categoryId" column="category_id"/>
        <result property="status" column="status"/>
        <result property="currentVersion" column="current_version"/>
        <result property="tags" column="tags"/>
        <result property="keywords" column="keywords"/>
        <result property="author" column="author"/>
        <result property="createdBy" column="created_by"/>
        <result property="updatedBy" column="updated_by"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="deleted" column="deleted"/>
        <result property="similarityScore" column="similarity_score"/>
    </resultMap>

    <!-- 全文检索：使用 PostgreSQL tsvector + tsquery，支持中文分词（需 zhparser 扩展）-->
    <select id="fullTextSearch" resultMap="specResultMap">
        SELECT id, category_id, title, content, keywords, tags, status, current_version, created_at, updated_at, deleted FROM spec
        WHERE deleted = false
          AND status = 'active'
          AND to_tsvector('simple', coalesce(title,'') || ' ' || coalesce(content,'') || ' ' || coalesce(keywords,''))
              @@ plainto_tsquery('simple', #{query})
        <if test="categoryId != null">
            AND category_id = #{categoryId}
        </if>
        ORDER BY ts_rank(
            to_tsvector('simple', coalesce(title,'') || ' ' || coalesce(content,'') || ' ' || coalesce(keywords,'')),
            plainto_tsquery('simple', #{query})
        ) DESC
        LIMIT #{limit}
    </select>

    <!-- 向量语义检索：cosine similarity via pgvector <=> operator -->
    <select id="vectorSearch" resultMap="specResultMap">
        SELECT s.*, (1 - (s.embedding &lt;=&gt; #{embedding}::vector)) AS similarity_score
        FROM spec s
        WHERE s.deleted = false
          AND s.status = 'active'
          AND s.embedding IS NOT NULL
          AND (1 - (s.embedding &lt;=&gt; #{embedding}::vector)) &gt; 0.3
          <if test="categoryId != null">
            AND s.category_id = #{categoryId}
          </if>
        ORDER BY s.embedding &lt;=&gt; #{embedding}::vector
        LIMIT #{limit}
    </select>

    <!-- 更新 embedding 向量 -->
    <update id="updateEmbedding">
        UPDATE spec SET embedding = #{embedding}::vector WHERE id = #{specId}
    </update>

</mapper>
