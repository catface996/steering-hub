<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.steeringhub.spec.mapper.SpecUsageMapper">

    <select id="selectByRepoId" resultType="com.steeringhub.spec.entity.SpecUsage">
        SELECT * FROM spec_usage
        WHERE repo_id = #{repoId}
        ORDER BY used_at DESC
    </select>

    <select id="selectBySpecId" resultType="com.steeringhub.spec.entity.SpecUsage">
        SELECT * FROM spec_usage
        WHERE spec_id = #{specId}
        ORDER BY used_at DESC
    </select>

    <select id="selectUsageStats" resultType="java.util.Map">
        SELECT
            su.spec_id AS specId,
            s.title AS specTitle,
            COUNT(*) AS usageCount,
            MAX(su.used_at) AS lastUsedAt
        FROM spec_usage su
        JOIN spec s ON s.id = su.spec_id
        GROUP BY su.spec_id, s.title
        ORDER BY usageCount DESC
        LIMIT #{limit}
    </select>

</mapper>
