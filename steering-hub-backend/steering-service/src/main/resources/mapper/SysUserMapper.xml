<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.steeringhub.steering.mapper.SysUserMapper">

    <select id="findByUsername" resultType="com.steeringhub.steering.entity.SysUser">
        SELECT id, username, password, nickname, role, enabled, created_at, updated_at
        FROM sys_user
        WHERE username = #{username}
        LIMIT 1
    </select>

</mapper>
